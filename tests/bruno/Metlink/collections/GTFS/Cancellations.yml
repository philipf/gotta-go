info:
  name: Cancellations
  type: http
  seq: 6

http:
  method: GET
  url: https://{{base-uri}}/trip-cancellations
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Agencies
  type: http
  seq: 1

http:
  method: GET
  url: https://{{base-uri}}/gtfs/agency
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Frame
  type: http
  seq: 1

http:
  method: GET
  url: http://{{base-uri}}/v1/frame
  headers:
    - name: X-Radiator-Slug
      value: "{{radiator-slug}}"
    - name: Accept
      value: image/bmp
    - name: Accept-Encoding
      value: gzip
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

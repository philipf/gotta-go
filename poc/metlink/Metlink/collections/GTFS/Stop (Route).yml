info:
  name: Stop (Route)
  type: http
  seq: 2

http:
  method: GET
  url: https://{{base-uri}}/gtfs/stops?route_id=10
  params:
    - name: route_id
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

settings.h
settings.*.h
!settings.example.h
build/

# Generated by gen_compile_commands.py — machine-specific absolute paths
# and build-cache hashes. .clangd is the portable config; this is not.
compile_commands.json
